seed: 42

dataset:
  name: mnist
  root: ./data
  image_size: 28
  channels: 1

centralized:
  rounds: 180
  max_steps_per_round: 80

model:
  architecture: unet
  base_channels: 40
  channel_multipliers: [1, 2, 4, 8, 8]
  blocks_per_level: 2
  lora_rank: 4
  lora_alpha: 4.0

diffusion:
  timesteps: 500
  sample_steps: 500
  sampler: ddpm
  ddim_eta: 0.0
  clip_denoised: true
  x0_loss_weight: 0.1

training:
  batch_size: 128
  lr: 0.0001
  num_workers: 0
  weight_decay: 0.0
  ema_decay: 0.999
  unfreeze_param_prefixes:
    - "*"

evaluation:
  num_real_samples: 384
  num_fake_samples: 384
  eval_batch_size: 128
  proxy_feature_size: 8
  semantic_eval_every: 5
  evaluator_cache_dir: ./outputs/evaluators

system:
  device: auto
  output_dir: ./outputs/mnist_b40x5_decoder_x0_centralized_t500_repro_clean_run
  save_every_round: false
  save_final_samples: true
